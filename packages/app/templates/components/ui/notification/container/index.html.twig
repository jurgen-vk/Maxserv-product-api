{% include 'components.ui.notification' only %}

<div {{ attrs(
  defaults: {
    class: 'notification-container',
    'data-server-notifications': notifications.all()|json_encode
  },
  attributes: attributes
) }}>

</div>
