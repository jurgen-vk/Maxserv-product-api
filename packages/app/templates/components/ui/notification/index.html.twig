<template id="notification-template">
  <div {{ attrs(defaults: { class: 'notification' }, attributes: attributes) }}>
    <span class="icon-slot"></span>
    <span class="message"></span>
    <button type="button" class="close" aria-label="Dismiss">{{ icon('lucide:x') }}</button>
  </div>
</template>
