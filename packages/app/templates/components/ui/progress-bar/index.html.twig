{% types {
  attributes: 'array',
  variant?: 'string|null',
} %}
{% set variant = variant|default(null) %}

<div {{ attrs(defaults: { class: 'c_ui_progress-bar', 'data-variant': variant }, attributes: attributes) }}>
  <div class="fill"></div>
</div>
