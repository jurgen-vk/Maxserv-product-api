{% types {
  attributes?: 'array|null',
} %}
{% set attributes = attributes|default(null) %}

<div {{ attrs(
  defaults: {
    class: 'c_ui_toaster',
    'data-server-notifications': notifications.all()|json_encode
  },
  attributes: attributes
) }}>
  {% include 'components.ui.toaster._toast' only %}
</div>
