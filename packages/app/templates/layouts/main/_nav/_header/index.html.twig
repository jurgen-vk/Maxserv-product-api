<div class="nav-header">
  <button type="button" class="hamburger-toggle" popovertarget="nav-sidebar" popovertargetaction="toggle"
          aria-label="Toggle navigation">
    <span class="hamburger-icon">{{ icon('lucide:menu') }}</span>
    <span class="close-icon">{{ icon('lucide:x') }}</span>
  </button>
  <a href="{{ path('home_index') }}" class="logo-link">
    {% include 'components.ui.brand.logo' %}
  </a>
</div>
